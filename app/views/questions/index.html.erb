<h1>Listing Questions</h1>
 <%= link_to 'Ask a New Question', new_article_path %>

<table>
  <tr>
    <th>Title</th>
    <th>Text</th>
  </tr>
 
  <% @question.each do |question| %>
    <tr>
      <td><%= question.title %></td>
      <td><%= question.text %></td>
    </tr>
  <% end %>
</table>